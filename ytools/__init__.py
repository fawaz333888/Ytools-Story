"""Ytools — YouTube faceless automation pipeline.

Pipeline: story generation -> edge-tts narration (word timings) ->
overlay pre-render (particles, watermark, karaoke subtitles) ->
ffmpeg compose (loop footage, mute, burn overlays, mux audio).
"""

__version__ = "0.1.0"
